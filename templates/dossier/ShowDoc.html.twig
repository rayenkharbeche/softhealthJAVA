{% extends 'base.html.twig' %}
{% block title %} {% endblock  %}

{% block body %}
    <div class="m-4">
        <h2 style="color: #22aab1;">Liste des dossiers</h2>
    </div>
    <table class="table table-striped table-hover">
        <thead style="backgournd: #efefef;"> <!-- hedhi yaani table headers -->
        <tr>
            <th>id</th>
            <th>update</th>
            <th>remove</th>
        </tr>
        </thead>
        <tbody> <!-- table body -->
        <form method="post" action="{{ path('recherche') }}">
        <label>Rechercher</label>
        <input type="text" name="search"> <input type="submit"value="Recherche">
        </form>
        <br> <br> <br> <br>
        {% for c in dossiers %}
            <tr>
                <td> {{ c.id}}</td>
                <td><a href="{{ path('edit_dossier',{'id':c.id}) }}">update</a></td>
                <td><a href="{{ path('remove_dossier',{'id':c.id}) }}">Remove</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock  %}
